name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    
    - name: Setup Racket
      run: | 
        apt-get update
        apt-get install -y racket
        raco pkg install --auto 2tdp
    - name: Run racket files
      run: |
        racket aoc-day1.rkt
